{
  "concepts": {
    "llm-backend-seam": {
      "confidence": 0.8,
      "contracts": [
        "LLMBackend.check_available() fails fast with a clear message if the backend can't be used right now (missing binary, Ollama not running, model not pulled).",
        "LLMBackend.bind_tools(tools) returns an object with .invoke(messages) -> AIMessage (with .tool_calls when the model wants to call a tool) -- this is the shape agent_node's ReAct loop requires, and it must be called exactly once per analyse() invocation then reused across every ReAct iteration (see agent.py's _build_graph/_make_agent_node), not once per iteration -- CLI backends rely on that single bound object surviving the whole loop to keep one --resume session alive instead of minting a fresh CLI session every turn.",
        "get_backend(name, model) is the only public factory; unknown names raise ValueError listing valid choices.",
        "For claude_cli/copilot_cli, tool schemas are described in the system prompt and the model is asked to reply with {\"tool\": name, \"args\": {...}} or {\"final_answer\": ...} -- _CLIToolCallingModel parses that JSON and translates it into a LangChain-shaped AIMessage so agent.py's graph doesn't need to know which backend is active."
      ],
      "created_at": "2026-08-03T02:48:55.229769+00:00",
      "description": "Pluggable chat-backend abstraction for splunk/agent.py's LangGraph ReAct loop, selected via config.AGENT_BACKEND (SPLUNK_AGENT_BACKEND env var). Three backends: 'ollama' (native LangChain tool-calling via ChatOllama), 'claude_cli' and 'copilot_cli' (shell to `claude -p`/`copilot -p` non-interactively with the CLI's own tool use disabled, bridging tool-calling by hand via a JSON protocol -- see splunk/cli_tool_protocol.py).",
      "evidence": [
        "splunk/llm_backends.py:_CLIToolCallingModel",
        "splunk/llm_backends.py:ClaudeCLIBackend",
        "splunk/llm_backends.py:CopilotCLIBackend",
        "splunk/cli_tool_protocol.py",
        "splunk/agent.py:_build_graph",
        "splunk/agent.py:_make_agent_node",
        "tests/test_llm_backends.py"
      ],
      "invariants": [
        "Session state (self._session_id, self._sent_message_signatures on _CLIToolCallingModel) is scoped to one analyse() call and discarded after -- unlike bee-bug-hunter's disk-persisted multi-role session store, agent.py has a single role and analyse() is one-shot per investigation, so no cross-process persistence or session forking is needed here.",
        "Claude CLI supports --append-system-prompt (system and user text sent separately); Copilot CLI does not and folds both into one combined -p argument -- this asymmetry is handled per-backend in llm_backends.py, not in the shared cli_tool_protocol.py."
      ],
      "last_validated": "2026-08-03T02:56:18.888178+00:00",
      "module": "splunk/llm_backends.py",
      "name": "llm-backend-seam"
    },
    "standalone-agent-langgraph": {
      "confidence": 0.8,
      "contracts": [
        "analyse(findings: dict) -> tuple[str, list[str]] is the sole public entry point; its return shape is a contract consumed by connector.py at two call sites and must not change without updating both.",
        "Domain-specific investigation guidance (prompts + SPL query templates) lives in splunk/investigation_areas.INVESTIGATION_AREAS, not hardcoded in agent.py's tool bodies — request_deeper_analysis and generate_followup_queries both read from it via get_prompt/get_spl_template.",
        "LogAnalysisState.hypotheses tracks root-cause candidates registered via rank_hypotheses (status starts 'open'); format_report's confirmed_hypothesis arg marks one confirmed and all other open ones rejected. This state is internal to a single analyse() graph invocation only — it does not persist across the outer per-iteration analyse() calls in connector.py's investigation loop.",
        "Tool call arguments (not tool return strings) are the channel for state mutation in tool_node_fn — it reads args from the preceding AIMessage.tool_calls, matched by tool_call_id, rather than parsing sentinel markers out of a tool's return value (format_report's return value is used verbatim as the final report, so it must stay clean)."
      ],
      "created_at": "2026-08-03T02:43:16.420467+00:00",
      "description": "LangGraph ReAct agent (Qwen2.5 via Ollama) used only behind the --investigate CLI flag / connector.py's run_standalone_agent path — the fallback investigation loop for environments without Copilot/Claude driving MCP directly. Reasons over detector findings via a small tool set and emits a markdown report through format_report.",
      "evidence": [
        "splunk/agent.py:LogAnalysisState",
        "splunk/agent.py:tool_node_fn",
        "splunk/agent.py:should_continue",
        "splunk/investigation_areas.py:INVESTIGATION_AREAS",
        "tests/test_agent.py"
      ],
      "invariants": [
        "should_continue ends the loop when iterations >= AGENT_MAX_ITER OR when all tracked hypotheses are resolved (non-'open'), whichever comes first.",
        "Adding a new investigation domain requires only a new entry in INVESTIGATION_AREAS, not edits to agent.py's tool logic."
      ],
      "last_validated": "2026-08-03T02:43:16.420515+00:00",
      "module": "splunk/agent.py",
      "name": "standalone-agent-langgraph"
    }
  },
  "meta": {
    "updated_at": "2026-08-03T02:56:18.888287+00:00"
  }
}
